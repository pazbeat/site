-- Признак «номер сертификата задан вручную» (2026-09-04).
--
-- Ручной выпуск позволяет задать номер, который уже на руках у клиента.
-- Подменять такой номер при коллизии в Altegio нельзя — но автоповтор синка
-- вызывается позже и без контекста, в котором сертификат создавался. Значит
-- признак должен жить в самой записи, а не в аргументе вызова.

ALTER TABLE "certificates"
  ADD COLUMN "serial_manual" BOOLEAN NOT NULL DEFAULT false;
