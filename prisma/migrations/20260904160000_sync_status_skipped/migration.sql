-- Статус «в CRM намеренно не писали» (2026-09-04).
--
-- Появился вместе с ручным выпуском сертификата из админки: бывает, что
-- сертификат в Altegio уже существует (продан действующим сайтом), и заводить
-- его там второй раз нельзя. Отдельно от `pending`, за которым сверка гоняется
-- каждые десять минут, и от `missing`, который означает «был и пропал».

ALTER TYPE "AltegioSyncStatus" ADD VALUE IF NOT EXISTS 'skipped';
