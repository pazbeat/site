-- Признак «карта показана серой». Без него переход «действителен → отозван»
-- с неизменным остатком (блокировка, возврат) не отличить от «ничего не
-- изменилось», и пуш такому устройству не уйдёт.
ALTER TABLE "wallet_passes" ADD COLUMN "shown_voided" BOOLEAN NOT NULL DEFAULT false;
