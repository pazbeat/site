-- Отпечаток текста правовой редакции.
--
-- Согласие покупателя ссылается на редакцию по номеру. Само по себе это не
-- доказывает, что текст с тех пор не правили. Отпечаток снимается при
-- создании редакции и сверяется с пересчитанным по содержимому.
--
-- Колонка nullable: у редакций, заведённых до этой миграции, отпечатка нет —
-- он проставляется отдельным скриптом (scripts/backfill-legal-hashes.ts).
ALTER TABLE "legal_versions" ADD COLUMN "content_sha256" TEXT;
