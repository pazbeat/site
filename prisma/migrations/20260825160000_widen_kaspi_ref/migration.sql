-- Расширить колонку короткого номера.
--
-- Заводилась под номер в семь знаков, но формат восстановили по квитанциям
-- действующего сайта — там двадцать цифр, и наш номер сделан такой же длины.
-- Колонку расширить забыли, оформление заказа падало на LengthMismatch.
-- Запас до 24 — на случай, если формат ещё уточнится.
ALTER TABLE "orders" ALTER COLUMN "kaspi_ref" TYPE VARCHAR(24);
