# Где мы сейчас

Живой снимок состояния проекта: что готово, что в работе, что чем заблокировано.
**Обновлять при смене состояния** — этот файл читает новая сессия, чтобы понять,
на чём остановились, вместо истории переписки (она локальная и не переносится).

Последнее обновление: 2026-08-24. Рабочая ветка — `redesign`.

**Полная проверка сайта пройдена 2026-08-21** — публичная часть, конструктор
по шагам, админка под обеими ролями, фоновые задания, безопасность. Найденное
и исправленное: бэкап отдавал битые файлы за рабочие, `AUTH_URL` за прокси
уводил Auth.js на localhost. Что осталось доработать — в конце файла.

**Сквозная проверка 2026-08-24** — весь сайт целиком: сборка, типы, линт,
251 тест, 41 адрес во всех трёх языках, ресурсы страниц, мобильная вёрстка,
покупка от конструктора до сертификата, выпуск в Altegio, письмо, PDF,
кошелёк, `/check`, ограничение частоты, заголовки безопасности, защита
админки, бэкапы, фоновые задания. **Найдено и исправлено:**

1. **Кнопка «Добавить в кошелёк» уводила на `localhost:3000`** — маршрут-
   диспетчер брал адрес из `request.url`, а за обратным прокси там внутренний
   адрес контейнера. Ошибка была в коде с самого его появления, то есть
   кнопка не работала ни разу. Чиним через `publicOrigin`, закрыто тестом.
2. **Переключатель языка терял строку запроса** — на `/pay/kaspi?order=…` и
   `/success?token=…` клик по KK или EN вёл в 404 с потерей оплаченного
   заказа. Свитчер вынесен в отдельный компонент (`useSearchParams` требует
   Suspense-границы) и переносит параметры.
3. **Оплаченный заказ на `/pay/kaspi` отдавал 404** вместо сертификата —
   ловил каждого, кто вернулся назад или обновил вкладку после оплаты.
4. **Бэкапы втрое распухли** от схемы `pgboss` (журнал заданий, ~7 тыс. строк
   в сутки). Исключена из дампа — восстанавливать её незачем.
5. `components/locale-switcher.tsx` был мёртвым кодом — переиспользован
   под п.2. Линт вычищен до нуля ошибок.

**Проверено и оказалось в порядке:** валидация всех публичных API, отсутствие
персональных данных в ответе `/check`, ограничение 5 запросов в минуту,
полнота переводов (KK/EN — все 338 ключей), отсутствие битых ссылок и
картинок, мобильная вёрстка от 375px без горизонтальной прокрутки, целостность
всех шести бэкапов, работа всех фоновых заданий.

**Замечено, не исправлено** (решать заказчику): `SENTRY_DSN` на сервере не
задан — мониторинг ошибок выключен; номинал 100 ₸ «ТЕСТ» открыт публично;
`PREVIEW_NO_2FA=1` снимает обязательный TOTP; Playwright описан в документации,
но конфига и команды `test:e2e` в проекте нет; синк в Altegio запускается
без `await` и без очереди — при падении процесса сертификат молча не доедет
до CRM, повтора нет.

**Доказуемость согласия — разобрано и доработано 2026-08-25.** Заказчик принёс
юридическую консультацию (логирование, хеш текста, оформление интерфейса, пункт
об электронной подписи, нотариальный осмотр). Проверено пять направлений,
23 находки подтверждены, 27 отсеяны как неточные. Сделано:

- **Согласие фиксирует редакцию, которую покупатель видел.** Записывался
  `currentVersionId` — всегда русская редакция, даже когда человек читал
  казахскую. Проверено живьём: казахский заказ теперь пишет оферту №14, а не
  №13, и запоминает язык.
- **Сохранение перевода в админке подменяло действующую оферту для всех
  языков** — указателем теперь распоряжается только русский текст.
- **Отпечаток текста (SHA-256)** у каждой редакции и в записи согласия;
  24 существующие редакции проставлены `scripts/backfill-legal-hashes.ts`.
- **Предъявление доказательств:** карточка заказа показывает User-Agent, язык
  и принятые редакции ссылками на `/admin/legal/version/[id]` с полным текстом
  и сверкой отпечатка; история версий не обрезана; поиск заказа работает по
  его номеру (тому, что уходит в чек Kaspi).
- **Корпоративная заявка** собирала контакт живого человека вообще без записи
  согласия — добавлены галочка со ссылками, серверная проверка и запись.
- **Модалка больше не рендерится поверх работающего конструктора** — за ней
  физически ничего нет. Оплатить в обход было нельзя и раньше, но пройти в
  конструктор клавишей Tab — можно, а это опровергало бы нотариальный протокол.
- Плейсхолдер «Привет, ты точно хочешь купить?» убран из сида.

**Осталось за заказчиком (юрист):** пункт о признании галочки простой
электронной подписью (сейчас акцепт опирается на ст. 396 ГК РК и описан в
оферте подробно — формально этого достаточно); политика конфиденциальности не
покрывает фактический состав данных (перечисляет ФИО, телефон и дату рождения,
которых сайт не собирает, и молчит про email, IP, User-Agent, cookie и передачу
в Resend и Altegio); нет пункта о приоритете русской редакции при расхождении
переводов. Нотариальный осмотр и заключение техспециалиста — после этого.

**Сервер проверен 2026-08-22** (только чтение): три контейнера здоровы, сайт
отвечает 200 и снаружи через Cloudflare, и напрямую на origin, мост Kaspi жив,
бэкапы пишутся и проходят проверку целостности, в логах за сутки чисто. База
пустая — 0 заказов, 0 сертификатов. Код на сервере — `297bb34`, от вершины
`redesign` отличается одной строкой в `AGENTS.md`, пересобирать незачем.
На витрине **всё ещё активен номинал 100 ₸ «ТЕСТ»** — снять перед запуском.
Единственное, что было на сервере и чего не было в гите, — две строки
`X-Forwarded-Host` в боевом nginx (без них Auth.js уводит вход на localhost).
Конфиг забран в `deploy/nginx/` побайтово, вместе с `ip-preview.conf`.

## Перед запуском — снять предохранители

Список заведён 2026-08-26 по решению заказчика: «пока не делай, тестовый
заказ пусть будет, я буду тестировать». То есть эти пункты снимать **не
раньше** окончания проверок — без них тестировать нечем.

| Что | Где | Почему важно |
|---|---|---|
| Номинал 100 ₸ «ТЕСТ» | админка → Номиналы | сейчас его может купить любой посетитель |
| `PAYMENT_MOCK=1` | `.env.production` | демо-оплата; видна только админу, но в проде ей не место |
| `PREVIEW_NO_2FA=1` | `.env.production` | вход в админку без второго фактора |
| `SENTRY_DSN` не задан | `.env.production` | мониторинг ошибок выключен, сбои видны только в логах |
| Стенд открыт поисковикам | `app/robots.ts` | `new.imbir.kz` конкурирует с `sert.imbir.kz` за выдачу |
| Тестовые заказы и сертификаты | БД | чистить, но **счётчик `Salon.lastCertSerial` НЕ сбрасывать**: номер уже занят в Altegio, повтор даст «already exists» |
| Cloudflare Full (strict) | панель Cloudflare | `deploy/lock-to-cloudflare.sh` |
| Сменить пароль Altegio и ключ Resend | менеджер паролей | светились в переписке |

**Altegio-режим уже боевой** (`ALTEGIO_TEST=0`, снят 2026-08-26 после первой
настоящей оплаты): продажа проводится по кассе с отметкой «Оплачено», пометки
`[ТЕСТ]` больше нет, а несмаппленный номинал теперь честно падает с ошибкой
вместо подстановки чужого тест-товара. Следствие: **любая проверочная покупка
пишет настоящее поступление в кассу** — отменять вручную в Altegio.

## Что работает

- **Фазы 1–2 закрыты**: каталог, конструктор, оплата, PDF с QR, доставка на
  email, админка, промокоды, отложенная отправка, напоминания.
- **Доставка только на email.** WhatsApp (ChatApp) убран из способов доставки
  по решению заказчика, тексты сайта переписаны.
- **Фаза 3 (Altegio)**: **включён и проверен на боевом стенде 2026-08-21** —
  покупка на 100 ₸ прошла до записи в CRM. Сертификат с телефоном покупателя
  найден в Altegio: статус Activated, баланс 100 из 100, тип «Тестовый 100
  тенге». Работает в тест-режиме (`ALTEGIO_TEST=1`): записи помечены `[ТЕСТ]`,
  продажа по кассе не проводится. Прод-маппинг филиалов и номиналов готов,
  `Salon.altegioLocationId` заполнен по всем 8 рабочим филиалам.
  Каталог `lib/altegio/catalog.ts` **точнее**, чем `spr.js` заказчика — при
  сверке с их файлами ничего оттуда не переносить, наши значения верные.
  Пересверено 2026-08-22 против свежего экспорта их админки `/admin/prices`:
  расходятся 9 номиналов, и по каждому запрошен живой Altegio — прав наш
  во всех девяти. У них на боевом сайте из-за этого выдаётся не то, что купили:
  на «Глории» и «Шаныраке» 140 000 → сертификат на 150 000, 150 000 → на
  200 000, а 200 000 → на 140 000 (покупатель теряет 60 тыс.); в Семее
  90 000 → на 100 000; на Мангилик товар для 70 000 у них вообще не существует;
  на Тәуелсіздік тестовый «100 ₸» выдаёт сертификат на 1 ₸. По программам
  расхождения только в Семее: у них другое семейство товаров («серт или
  депоз»), балансы сходятся везде, кроме «Гармония тела» — у них 20 000
  против наших 22 000.
- **Боевой стенд**: https://new.imbir.kz (сервер hoster.kz, Астана,
  185.129.51.231; Docker Compose + nginx + Let's Encrypt, за Cloudflare).

## Платежи

Вебхуков нет ни у одного провайдера — оплата подтверждается опросом либо
вручную из админки. Опрос страницы сначала смотрит статус заказа в нашей БД,
поэтому ручное подтверждение тоже доводит покупателя до `/success`.

| Способ | Состояние |
|---|---|
| Kaspi | наша часть готова; ждём проксирование на стороне действующего сайта |
| Оплата картой (ForteBank) | код готов и выверен по исходникам; **нет логина/пароля** — самый короткий путь к приёму денег |
| Halyk ePay | не начат — ждём реквизиты от банка |

**Про Kaspi, главное.** Ссылка и QR несут только номер заказа, суммы в них нет.
Название услуги и сумму приложение Kaspi запрашивает у бэкенда мерчанта — это
их стандартный протокол сервисов. На сайте заказчика это работает (QR сразу
показывает «Новый Электронный 20000, итого 20000»), у нас — «проверьте
правильность ввода данных», потому что Kaspi спрашивает про наш заказ старый
сайт. Ни регистрация заказа через их `POST /pay/{id}/kaspi`, ни смена формата
номера на UUID не помогают — проверено живьём 2026-08-21.

**Решение — проксирование** (предложил Тимур 2026-08-21): бэкенд действующего
сайта, не найдя заказ у себя, спрашивает нас, а после оплаты сообщает нам о
ней. Протокол Kaspi целиком остаётся на его стороне. Наша часть готова и
выкачена — два вызова, `docs/KASPI-BRIDGE.md`; секрет `KASPI_BRIDGE_TOKEN`
задан на сервере. **Ждём правку на стороне `sert.imbir.kz`.**

Долгосрочно правильнее, чтобы **Kaspi перенаправил сервис 11079 на наш домен**
(или завёл второй сервис) — тогда старый сайт можно выключить. Это же закроет
и автоподтверждение штатно: уведомление Kaspi об оплате — и есть недостающий
вебхук.

Подробности реализации и что именно выверено живьём — в
[AGENTS.md](../AGENTS.md), раздел «Оплата».

**Тестовая покупка на 100 ₸** включается скриптом
`npx tsx scripts/apply-test-100.ts` (номинал + программа), выключается тем же
скриптом с аргументом `off`. Товар-сертификат на 100 ₸ заведён в Altegio
только на Мангилик и Тәуелсіздік.

## В работе

### Кошелёк (Apple Wallet + Google Wallet)

Задача: сертификат добавляется в кошелёк, при погашении баланс уменьшается,
использованный помечается как погашенный.

- [x] Схема `WalletPass` + `WalletDevice`, миграция `20260813120000_wallet_passes`
- [x] Доменный слой: поля пропуска, баланс, признак `voided` (чистые функции + тесты)
- [x] Генерация `.pkpass` за интерфейсом провайдера + мок без ключей —
      `lib/wallet/`. Архив складывается своим ZIP-писателем, без новых
      зависимостей: библиотека действующего сайта `@walletpass/pass-js` под
      AGPL, а это лицензия, которая цепляется и к сетевому использованию.
      Мок пишет настоящий, но неподписанный `.pkpass` в `.wallet-outbox/` —
      распаковывается штатным архиватором, манифест сходится. Подпись через
      `openssl smime` (добавлен в образ) включится сама, как только появятся
      сертификат и WWDR
- [x] Apple Wallet Web Service: 4 эндпоинта — `app/api/wallet/apple/v1/`.
      Регистрация и отписка устройства, список изменившихся карт, выдача
      свежего пропуска, приём жалоб телефона в лог. Закрыты токеном пропуска
      (`Authorization: ApplePass`), сравнение постоянного времени. Пока нет
      сертификата Apple, выдача пропуска отвечает 503, а не отдаёт
      неподписанный файл. Миграция `20260822090000_wallet_shown_voided`
      добавляет `shown_voided`: без него отзыв карты с неизменным остатком
      не отличить от «ничего не изменилось» и пуш не уйдёт
- [x] Пуш через APNs — `lib/wallet/apns.ts`. HTTP/2 и подпись ES256
      средствами самого Node, без библиотек; JWT держится в памяти 50 минут,
      как требует Apple. Молчит, пока нет Auth Key `.p8` — телефон тогда
      приходит за обновлением сам, просто заметно реже
- [x] Google Wallet: ссылка добавления + PATCH объекта при погашении —
      `lib/wallet/google*.ts`. Ссылка «Сохранить» это подписанный JWT с картой
      внутри: ни оформление, ни карту заранее создавать не нужно, Google
      заводит их при первом сохранении. Обновление остатка — PATCH к их API,
      рассылку по устройствам Google берёт на себя, свой APNs не нужен.
      Тип `giftCard` — родное отображение остатка. Погашенную карту у Google
      можно закрыть по-настоящему (уезжает в неактивные), Apple умеет только
      покрасить серым. Кнопка независима от Apple: ключи разные
- [x] Связка с `lib/altegio/redemptions.ts` + кнопка в интерфейсе. Сверка
      погашений, изменив остаток, сама будит карту (`lib/wallet/notify.ts`);
      ответ 410 от Apple вычищает устройства, с которых карту удалили.
      Кнопка «Добавить в Apple Wallet» на странице успеха показывается,
      только когда есть чем подписать, — иначе телефон отверг бы файл

**Google Wallet работает** — подключён и проверен живьём 2026-08-24: кнопка
на странице успеха, ссылка подписывается, класс создан в Google и одобрен.
Ограничение одно, и оно на стороне кабинета: пока не запрошена публикация,
карту сохранят только аккаунты из списка тестовых.

Apple ждёт ключей: без сертификата кнопка не показывается, потому что
неподписанный пропуск телефон всё равно отвергнет. Что завести — ниже.

Ключ сервисного аккаунта хранится в env **в base64**: docker-compose
разворачивает `
` в env-файле в настоящий перенос и обрывает значение на
первой строке — ключ доезжал огрызком в 27 символов.

**Сертификат подписи есть, но просрочен.** Из исходников заказчика извлечены
Pass Type ID `pass.kz.imbir.sert` (Team `8P2AARWSYR`) и приватный ключ — лежат
в `secrets/` (папка в `.gitignore`, см. `secrets/README.md`). Срок действия
истёк 23.02.2026, владелец — CONNECTED HOME LTD., не «Имбирь». Нужен
перевыпуск в Apple Developer.

**Важно про Apple:** карта обновляется только если в пропуске есть
`webServiceURL` и `authenticationToken`. У текущего сайта заказчика их нет —
там пропуск статический, баланс на нём замирает навсегда. Удалить карту из
кошелька программно нельзя: используем `voided`, карта сереет.

**Источник данных для баланса готов** — `lib/altegio/redemptions.ts` каждые
15 минут тянет остаток из CRM, в базе есть `balanceKzt` и статусы.

## Чем заблокировано

| Что | Чего ждём |
|---|---|
| Оплата картой | логин и пароль ForteBank (Basic). В экспорте Node-RED их нет — лежат в зашифрованном `flows_cred.json` на сервере заказчика |
| Kaspi | правка на бэкенде `sert.imbir.kz` — проксировать к нам (`docs/KASPI-BRIDGE.md`, наша часть готова). Дальше — перенастройка сервиса 11079 на наш домен со стороны Kaspi, чтобы отвязаться от старого сайта |
| Halyk ePay | ClientID, ClientSecret, TerminalID от банка (`Epay@halykbank.kz`) |
| Apple Wallet | аккаунт Apple Developer на «Имбирь» (99 $/год; текущий сертификат просрочен 23.02.2026 и выписан на подрядчика). Оттуда: сертификат Pass Type ID + приватный ключ, промежуточный WWDR, Auth Key `.p8` для пушей, Team ID |
| Google Wallet | **подключён 2026-08-24.** Эмитент `3388000000023192690`, сервисный аккаунт `imbir-wallet@imbir-wallet.iam.gserviceaccount.com`, класс `…​.imbir-gift` создан и одобрен Google. Осталось одно: кабинет в **деморежиме** — карту сохранят только аккаунты из списка тестовых. Для всех покупателей нужно в кабинете заполнить профиль компании и запросить доступ для публикации |
| Замок на Cloudflare | **ждать нечего** (проверено 2026-08-22): origin отдаёт валидный Let's Encrypt на `new.imbir.kz` до 18.11.2026, Full (strict) включится без простоя. Переключить SSL/TLS домена, потом запустить `deploy/lock-to-cloudflare.sh` |

Тестовые реквизиты Halyk просить не нужно — они публичные:
https://epayment.kz/docs/Test-credentials

## Требования Halyk, влияющие на код

- `invoiceId` — только цифры, 6–15 знаков, уникальность по последним 6 цифрам.
  У нас заказы с cuid, понадобится отдельный числовой счётчик.
- `postLink` обязателен — это наш URL, куда банк шлёт результат оплаты.
  То есть вебхук есть, в отличие от Kaspi и Forte.

## Перед боевым запуском

- [ ] Убрать `PREVIEW_NO_2FA=1` из окружения сервера
- [ ] `npx tsx scripts/apply-test-100.ts off` — снять тестовую покупку на 100 ₸
- [ ] Почистить тестовые заказы и сертификаты. **Счётчик номеров
      (`Salon.lastCertSerial`) при этом НЕ сбрасывать:** номер уже уехал в
      Altegio, и после сброса следующая продажа получит занятый номер. Altegio
      ответит «такой сертификат уже есть», мы посчитаем это идемпотентным
      успехом — и сертификат покупателя в CRM не появится. Поймано на своей же
      чистке 2026-08-24
- [ ] `ALTEGIO_TEST=0` и `ALTEGIO_SYNC=1` — боевая запись в CRM
- [ ] Перевыпустить пароль Altegio и ключ Resend (см. долги по безопасности)

## Что стоит доработать (по итогам проверки 2026-08-21)

Отсортировано по пользе. Ничего из этого не мешает запуску.

1. ~~**Обновить Next и next-auth.**~~ **Сделано и выкачено** — обновление уехало
   вместе с коммитом `7bc6ce9`. На сервере крутится Next 16.3.2 и next-auth
   5.0.0-beta.32 (проверено в контейнере 2026-08-22), критичных и высоких
   advisory по ним больше нет. Осталось 3 high в `deepmerge-ts` через
   `@prisma/config` — сборочная зависимость, не рантайм; `npm audit fix --force`
   откатил бы prisma до 6.12, поэтому не трогаем.
2. **Серверные ошибки никуда не сообщаются.** Sentry подключён только на
   клиенте (`NEXT_PUBLIC_SENTRY_DSN`), серверные падения видны лишь в логах
   контейнера, которые никто не читает. Добавить серверный DSN.
3. **Выключить у Cloudflare обфускацию почты** (Scrape Shield → Email Address
   Obfuscation). Она подменяет адрес в подвале на `[email protected]` и
   подставляет свой скрипт, который наш CSP блокирует — посетитель видит
   заглушку вместо `spa@imbir.kz`.
4. **Ни одна программа не ограничена по городам**, хотя механизм есть и
   работает (`Program.cities`, проверка и на клиенте, и на сервере). По ТЗ
   часть SPA доступна только в Астане и Алматы — нужен список от заказчика.
5. **Филиал на Тәуелсіздік выключен** (`active=false`, отключён из админки
   2026-08-20). В Altegio он заведён и товары на него есть — проверить, так ли
   это задумано.
6. **Публичные страницы отдаются без кеша** (`no-store`), поэтому Cloudflare
   их не кеширует. Для витрины это лишняя нагрузка и лишние миллисекунды.
7. **Чекбокс согласия на мобильном — 16px.** Это обязательное юридическое
   действие, попасть по нему пальцем трудно. Увеличить область нажатия.
8. **Бэкапы лежат на том же сервере.** Диск умрёт — умрут и они. Настроить
   отгрузку в другое место (S3, второй сервер).
9. **Ограничение частоты запросов живёт в памяти процесса** — сбрасывается на
   каждом перезапуске и не переживёт второй экземпляр приложения.
10. **Таблица заданий pg-boss растёт** (~7 тыс. строк в сутки, 2.6 МБ). Пока
    12 МБ на всю базу, но за год накопится — проверить архивацию.

## Аналитика источников — сделано 2026-08-25

Отчёт **«Источники»** в админке: заходы → открыли конструктор → покупки →
выручка, в разрезе каналов. Отвечает на исходный вопрос заказчика «кто из них
покупает», а не только «сколько зашло».

Как устроено:

- Канал определяется один раз в `proxy.ts` (`lib/source.ts` — чистый модуль,
  22 теста): метки `utm_*`, идентификаторы клика `gclid`/`yclid`/`fbclid`,
  иначе адрес страницы-источника. Закрытый словарь из 15 каналов — чтобы
  отчёт не превратился в свалку опечаток.
- Живёт в куке `imbir_src` (180 дней, `httpOnly`), помнит **кто привёл** и
  **кто закрыл**. Возврат с оплаты и переходы из веб-почты каналом не
  считаются — иначе продажу записало бы на Kaspi или Gmail, украв у рекламы.
- Заходы — дневной агрегат `visit_stats` («день × канал × стадия»), без IP,
  UA и идентификаторов. Публичного маршрута нет намеренно: открытый POST со
  счётчиком позволял бы накрутить знаменатель конверсии из терминала.
- Роботы и предзагрузка ссылок отсеиваются, иначе конверсия утонет.
- При покупке канал переносится в сам заказ (`Order.srcFirst/srcLast/…`),
  поэтому выручка по каналам сходится с «Продажами» до тенге.
- Добавлено `Order.paidAt` — время поступления денег; отдельно от `createdAt`,
  так как вебхуков у Kaspi и Forte нет и оплата приходит много позже.
- Попутно исправлено: границы суток для колонок типа DATE (`dayRangeFilter`) —
  прежний фильтр захватывал лишний день слева и терял день справа.

**Что нужно от заказчика, иначе каналов не будет видно:** все рекламные и
рассылочные ссылки размечать метками
`?utm_source=instagram&utm_medium=cpc&utm_campaign=8marta`. Без разметки
Instagram и реклама сольются в «переходы с других сайтов».

**Наши цифры не совпадут с Метрикой, и это нормально.** У нас last-touch по
своей куке и «покупка = `Order.status=paid` на сервере» — конверсия, которую
внешний счётчик увидеть не может в принципе (покупатель платит в приложении
Kaspi и может не вернуться на сайт). Расхождение в 10–30 % — признак
правильной работы; наши деньги сходятся с админкой.

### Осталось по аналитике и продвижению

1. **Счётчики** — Яндекс.Метрика и GA4, когда будут аккаунты. Шов: функция
   по образцу `sentryConnectSrc` в `lib/security.ts` + чтение nonce из
   заголовка `x-nonce`. В `script-src` домены добавлять бесполезно — там
   `strict-dynamic`.
2. **SEO**: микроразметки schema.org нет ни одной (Organization, LocalBusiness
   на 8 филиалов, Product на программы, FAQPage из `lib/guest-info.ts`).
   Правовым страницам не хватает canonical; у сезонных лендингов бедный
   Open Graph — а это именно те страницы, куда ведёт реклама. Страниц
   `/salons` и `/salons/[city]` нет — самые дешёвые посадочные под запросы
   «тайский массаж Астана».
3. **Стенд открыт для индексации** — `robots.txt` разрешает всё. До запуска
   `new.imbir.kz` стоит закрыть, иначе конкурирует с `sert.imbir.kz`.
4. **Выгрузка офлайн-конверсий** в Google Ads и Директ — данные уже собраны
   (`clickId` + `paidAt` + сумма), нужен только маршрут выгрузки.
5. Каналы продвижения на выбор заказчика: Instagram, Google Ads, Яндекс
   Директ, 2ГИС, письма по своей базе.

## Открытые решения

- `main` отстаёт от `redesign`. Сливать или нет — не решено.
- Запись `kling` в `.mcp.json` не закоммичена; на бесплатном тарифе бесполезна.

## Долги по безопасности

- **Пароль от Altegio был закоммичен** в `AGENTS.md` и остаётся в истории гита
  (коммит `f5f3d6d`), файл опубликован на GitHub. Удаление из файла его не
  отзывает — нужна смена пароля на стороне Altegio.
- **Ключ подписи Apple Wallet** лежал открытым текстом в `flows.json`
  (исходники текущего сайта заказчика). Всё равно просрочен — перевыпустить.
- Боевой ключ Resend когда-то присылался открытым текстом и лежит в тех же
  исходниках — перевыпустить перед боевым запуском.
